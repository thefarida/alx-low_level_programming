#include "main.h"

/**
 * _abs -> computes absolute values of integers
 * @n: parameter
 * Return: absolute value of a number or 0
 */
int _abs(int n)
{
if (n < 0)
{
int abs_val;

abs_val = n * -1;
return (abs_value);
}
return (n);
}
