#include "main.h"

/**
 * _islower -> checks if character is lowercse
 * @c: a character argument
 * Return: returns 1 and 0 depending on the conditions
 */
int _islower(int c)
{

return (c >= 'a' && c <= 'z');

}
