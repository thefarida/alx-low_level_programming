#include "main.h"
#include <stdio.h>

/**
 * print_rev -> Prints a string in reverse
 * @s: String to print
 *
 * Return: void
 */
void print_rev(char *s)
{
	int f = 0;

	while (s[f] != '\0')
	{
		f++
	}
	for (f -= 1; c >= 0; c--)
	{
		_putchar(s[f]);
	}
	_putchar('\n');
}
